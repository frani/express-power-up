{
  "Create Controller with GET": {
    "prefix": "ep-example-validation",
    "body": [
      "const Joi = require('joi')",
      "module.exports = {",
      "  /**",
      "   * GET /v1/${1:item}s",
      "   */",
      "  list: {",
      "    query: Joi.object({",
      "      page: Joi.number().min(1),",
      "      limit: Joi.number().min(1).max(100)",
      "    })",
      "  },",
      "",
      "  /**",
      "   * POST /v1/${1:item}s",
      "   */",
      "  post: {",
      "    body: Joi.object({",
      "      confirm: Joi.boolean().valid(true).required()",
      "    }).required()",
      "  },",
      "",
      "  /**",
      "   * GET /v1/${1:item}s/:${1:item}id",
      "   */",
      "  get: {",
      "    params: Joi.object({",
      "      ${1:item}id: Joi.string().min(8).max(20).required()",
      "    }).required()",
      "  },",
      "",
      "  /**",
      "   * PUT /v1/${1:item}s/:${1:item}id",
      "   */",
      "  put: {",
      "    params: Joi.object({",
      "      ${1:item}id: Joi.string().min(8).max(20).required()",
      "    }).required(),",
      "    body: Joi.object().required()",
      "  },",
      "",
      "  /**",
      "   * DELETE /v1/${1:item}s/${1:item}id",
      "   */",
      "  delete${1:item}: {",
      "    params: Joi.object({",
      "      ${1:item}id: Joi.string().min(3).max(60).required()",
      "    }).required(),",
      "  },",
      "}\n$0"
    ],
    "description": "Create an '*.validation.js' file content, importing Joi"
  }
}
